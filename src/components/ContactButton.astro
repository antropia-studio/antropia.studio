---
const { class: className, ...rest } = Astro.props;
---
<button
  class={`bg-white text-blinking-blue font-nudica text-xl px-10 py-2 rounded-full mt-10 hover:bg-blinking-blue hover:text-white cursor-pointer transition ${className}`}
  {...rest}>
  Contact us
</button>
