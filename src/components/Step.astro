---
const { class: className, title, ...rest } = Astro.props;
---

<p
  class={`text-6xl font-round8 tracking-wide font-light ${className}`}
  {...rest}
>
  {title}
</p>
<p class="text-lg xl:text-2xl font-thin">
  <slot />
</p>
