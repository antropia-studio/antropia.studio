---
const { class: className, ...rest } = Astro.props;
---

<div class={`h-px mt-24 w-screen ${className}`} {...rest} />

<div class="flex flex-row gap-10 px-10 py-20">
  <div class="flex flex-col flex-1">
    <slot name="first" />
  </div>
  <div class="flex flex-col flex-1">
    <slot name="second" />
  </div>
  <div class="flex flex-col flex-1">
    <slot name="third" />
  </div>
</div>

<div class={`h-px w-screen ${className}`} {...rest} />
